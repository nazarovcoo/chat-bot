# UX guard: auto text for file-only messages

## Что изменено
Файл: `admin.html`
Функция: `handleChatObSend`

## Детали
Добавлена нормализация отправляемого текста:
- Если пользователь прикрепляет файл, но оставляет поле ввода пустым, мы формируем `safeText = "📎 Файл"`.
- Этот текст передается в payload `fetch` запроса (`text: safeText`), предотвращая отправку пустого сообщения (`text: ""`), из-за которого LLM мог возвращать ошибку или пустой ответ.
- При этом визуально (в самом интерфейсе) мы не создаем лишний 'текстовый' bubble пользователя, если он ничего не ввел (используется `rawText`). Визуально отображается только стилизованный bubble состояния загрузки файла (`uploadBubble`).

## Почему это безопасно
1. Никакие контракты API Backend'а (`chatOsHandler`) не изменены (поля остаются те же).
2. Никакие пути Firestore не затронуты.
3. Логика отправки обычных сообщений (без файлов) и сложных сообщений (с текстом и файлом) полностью сохранена.
