# Анализ фронтенд кодовой базы: Прайсер товаров

## 📁 Структура проекта

```
bot/
├── unit-economics.html        # Главный файл — всё приложение (8 489 строк)
├── admin.html                 # Админ-панель (94 КБ)
├── calculator.html            # Отдельный калькулятор (2.2 МБ)
├── deploy/
│   └── index.html             # Копия unit-economics.html для публикации
├── netlify/
│   └── functions/
│       ├── claude-proxy.js    # Прокси к Anthropic API
│       └── telegram-notify.js # Отправка уведомлений в Telegram
├── netlify.toml               # Конфигурация хостинга и редиректов
├── deploy.sh                  # Скрипт деплоя (cp + netlify deploy)
├── CLAUDE.md                  # Правила для ИИ-ассистента
└── firebase.json              # Конфигурация Firebase
```

**Принцип организации:** монолитный подход — всё приложение в одном HTML-файле. Нет системы сборки, нет разделения на модули. CSS (~1 750 строк, строки 13–1763), HTML (~1 550 строк, 1764–3314), JavaScript (~5 175 строк, 3315–8489).

---

## 🛠 Технологический стек

| Технология | Версия | Назначение |
|---|---|---|
| Vanilla JavaScript | ES2022 | Вся логика приложения |
| Firebase Firestore | 9.23.0 (compat) | База данных в реальном времени |
| Firebase Auth | 9.23.0 (compat) | Email/Password + Google OAuth |
| Chart.js | 4.4.0 | Графики в аналитике |
| SheetJS (xlsx) | 0.20.3 | Импорт/экспорт Excel |
| Claude AI (Anthropic) | claude-opus-4-6 | OCR инвойсов, распознавание товаров |
| Netlify | — | Хостинг + Serverless Functions |
| CSS Custom Properties | — | Темизация (dark/light) |

**Без фреймворка.** Никаких React, Vue, Angular — чистый браузерный JS с прямой работой с DOM.

---

## 🏗 Архитектура

### Глобальное состояние

Всё приложение работает через глобальные переменные в одном `<script>`:

```javascript
let rows = [];          // Товары текущей партии
let archive = [];       // Архив удалённых товаров
let sales = [];         // История продаж
let salesCounter = 0;
let db = null;          // Firestore instance
let auth;               // Firebase Auth instance
let currentUser = null; // Авторизованный пользователь
let defaultRate = 12519; // Курс USD→UZS
```

### Изоляция данных по пользователям

```javascript
function col(name) {
  return db.collection('users').doc(currentUser.uid).collection(name);
}
// Использование: col('praicer'), col('wh_inventory'), col('sales')...
```

Каждый пользователь хранит данные в своём поддереве Firestore: `users/{uid}/{collection}`.

### Синхронизация с Firestore

Паттерн «отложенного сохранения» (debounce 2 секунды):

```javascript
let _saveTimer = null;
function scheduleSave() {
  clearTimeout(_saveTimer);
  _saveTimer = setTimeout(saveToFirestore, 2000);
}
```

Реальное время: `onSnapshot` слушатель обновляет UI при изменениях из других вкладок/устройств.

### Навигация

Ручная вкладочная навигация через `showTab(name)`:

```javascript
function showTab(name, btn) {
  document.querySelectorAll('.tab-content').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('.htab').forEach(b => b.classList.remove('active'));
  document.getElementById('tab-' + name).classList.add('active');
  // Синхронизация мобильной нижней навигации
  document.querySelectorAll('.mobile-nav-btn').forEach(b =>
    b.classList.toggle('active', b.dataset.tab === name));
  activeTab = name;
}
```

### Авторизация

Поток: `initAuth()` → `onAuthStateChanged` → если залогинен → `startFirebaseSync()` + `fetchExchangeRate()` + `initPinProtection()`. Staging-среда обходит авторизацию через проверку `location.hostname`.

---

## 🎨 UI/UX и стилизация

### Тёмная/светлая тема

Реализована через `data-theme` атрибут на `<html>` и CSS-переопределения:

```css
/* Dark (по умолчанию) */
.auth-card { background: #111422; border: 1px solid #1e2235; }

/* Light override */
[data-theme="light"] .auth-card { background: #fff; border-color: #e2e8f0; }
```

Тема сохраняется в `localStorage`.

### Цветовая палитра

| Роль | Цвет |
|---|---|
| Фон | `#0a0c12` |
| Карточки | `#111422` |
| Акцент (primary) | `#6366f1` (indigo) |
| Успех | `#22c55e` |
| Ошибка | `#ef4444` |
| Текст | `#e2e8f0` |
| Muted | `#4f5a7a` |

### Адаптивность

5 брейкпоинтов: `1100px`, `900px`, `768px`, `640px`, `480px`.

На мобиле (`≤768px`):
- Шапка: скрывается навигация и заголовок
- Появляется нижняя панель навигации (фиксированная, 5 вкладок)
- Модальные окна: слайдятся снизу (`align-items: flex-end`)
- Таблицы: горизонтальный скролл

```css
@media (max-width: 768px) {
  .header-nav { display: none !important; }
  .mobile-nav { display: flex; position: fixed; bottom: 0; }
  .page { padding-bottom: 80px !important; }
}
```

### Компоненты UI (самодельные)

- Toast-уведомления (`showToast`)
- Confirm-диалог (`showConfirm`)
- Lightbox для фото
- Drag & drop загрузка файлов
- Анимированный sync-индикатор

---

## ✅ Качество кода

### Сильные стороны

- **Консистентный стиль именования:** camelCase для функций, kebab-case для CSS-классов, `am-*` префикс для полей модала добавления
- **Defensive coding:** везде используется `?.` (optional chaining) и `|| ''` fallback
- **Escaping:** функция `escH(s)` для защиты от XSS при вставке в innerHTML
- **Изоляция данных:** per-user Firestore поддеревья

### Области для улучшения

- **Монолит:** 8 489 строк в одном файле — сложно поддерживать командой
- **Нет TypeScript:** типизация только через соглашения
- **Нет тестов:** изменения проверяются вручную на staging
- **Нет линтера:** ESLint/Prettier не настроены
- **Глобальное состояние:** все переменные в window-scope, риск конфликтов
- **Смешанная логика:** бизнес-логика, UI и работа с DOM не разделены

---

## 🔧 Ключевые компоненты

### 1. `saveToFirestore` / `loadFromFirestore` — ядро персистентности

```javascript
function saveToFirestore() {
  if (!currentUser) return;
  const payload = {
    rows, archive, sales, salesCounter,
    categories, countries, suppliers, sizeOptions,
    updatedBy: SESSION_ID,
    updatedAt: firebase.firestore.FieldValue.serverTimestamp()
  };
  col('praicer').doc('data').set(payload);
}
```

Всё состояние сохраняется как **один документ** в Firestore. SESSION_ID предотвращает циклические обновления.

### 2. `addRow(data)` — добавление товара

```javascript
function addRow(data) {
  const id = ++rowCounter;
  rows.push({ id, ...data });
  const tr = document.createElement('tr');
  tr.id = 'row-' + id;
  tr.innerHTML = `...`; // ~50 строк HTML
  tbody.appendChild(tr);
  recalc();
  scheduleSave();
  return id;
}
```

### 3. `extractFromPhoto` — AI-распознавание инвойсов

```javascript
async function extractFromPhoto(dataUrl, mimeType) {
  const resp = await fetch('/api/claude-proxy', { method: 'POST', body: JSON.stringify({
    model: 'claude-opus-4-6',
    messages: [{ role: 'user', content: [
      { type: 'image', source: { type: 'base64', data: base64 } },
      { type: 'text', text: 'Extract ALL products. Return JSON array...' }
    ]}]
  })});
  // Если 1 товар → заполняет форму
  // Если несколько → открывает модал массового импорта
}
```

### 4. `_recalcNow` — пересчёт юнит-экономики

Центральная функция (~200 строк): для каждой строки считает себестоимость, цену продажи, прибыль, маржу, ROI. Обновляет DOM-элементы по id (`cost-{id}`, `price-{id}` и т.д.).

### 5. `initAuth` — точка входа в приложение

```javascript
function initAuth() {
  auth = firebase.auth();
  if (location.hostname === 'praicer-staging.netlify.app') {
    currentUser = { uid: 'staging-preview-user', displayName: '🧪 Staging' };
    startFirebaseSync(); fetchExchangeRate(); initPinProtection();
    return;
  }
  auth.onAuthStateChanged(user => {
    if (user) { currentUser = user; /* запуск приложения */ }
    else { /* показ экрана авторизации */ }
  });
}
```

---

## 🚀 Инфраструктура

### Деплой

```bash
# deploy.sh — два окружения
cp unit-economics.html deploy/index.html

# Staging (черновик, без логина)
netlify deploy --prod --site 0fdd8d90... --dir deploy

# Production (основной сайт)
netlify deploy --prod --site 374224ae... --dir deploy
```

### Serverless Functions

**`/api/claude-proxy`** — безопасный прокси к Anthropic API. API-ключ хранится в переменных окружения Netlify, никогда не попадает в браузер.

**`/api/telegram-notify`** — отправка уведомлений в Telegram при событиях (низкий остаток товара и др.).

### CLAUDE.md

Файл с инструкциями для ИИ-ассистента — правило автодеплоя после каждого изменения кода.

---

## 📋 Выводы и рекомендации

### Сильные стороны проекта
- **Скорость запуска:** ноль настройки окружения — один HTML-файл открывается в браузере
- **Real-time sync:** Firestore onSnapshot даёт мгновенную синхронизацию между устройствами
- **AI-интеграция:** распознавание инвойсов через Claude Vision — нестандартное и полезное решение
- **Per-user изоляция:** грамотная архитектура данных через `users/{uid}/...`
- **Staging/Production:** чёткое разделение окружений

### Рекомендации при масштабировании
1. **Разбить на модули:** ES Modules или хотя бы отдельные JS-файлы по фичам
2. **Добавить TypeScript:** хотя бы для ключевых типов данных (Product, Sale, Warehouse)
3. **Настроить ESLint + Prettier:** стандартизация стиля кода
4. **Добавить базовые тесты:** хотя бы для `_recalcNow` и `saveToFirestore`/`loadFromFirestore`
5. **Разбить CSS:** отдельные файлы по компонентам

### Уровень сложности
**Middle-friendly** — логика понятна, но масштаб (8 500 строк) и отсутствие модульности требуют опыта навигации в больших монолитах.
