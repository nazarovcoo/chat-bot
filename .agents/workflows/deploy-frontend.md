---
description: Automatically deploy frontend changes to production (createbotaiagent.com)
---

Always remember that after making changes to the UI (`html`, `js`, `css`), the user expects to see them on the live website.

Run the following command to deploy frontend code to Firebase Hosting:

```bash
firebase deploy --only hosting
```
